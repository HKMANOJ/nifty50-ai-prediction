services:
  - type: web
    name: nifty50-ai-prediction
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 serve_nifty50_app.py --host 0.0.0.0 --port $PORT
    autoDeploy: true
